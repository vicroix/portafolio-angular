import { AppearOnScrollDirective } from './appear-on-scroll.directive';

describe('AppearOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
